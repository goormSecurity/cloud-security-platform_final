apiVersion: 1

datasources:
  - name: JSON API
    type: marcusolsson-json-datasource
    uid: afot70vnoy7swf
    url: http://json-api:5000
    access: proxy
    isDefault: true
    editable: false

  - name: Loki
    type: loki
    uid: loki-cloud-sec
    url: http://loki:3100
    access: proxy
    editable: false
    jsonData:
      maxLines: 1000
