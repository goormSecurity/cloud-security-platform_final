apiVersion: 1

datasources:
  - name: JSON API
    type: marcusolsson-json-datasource
    uid: afot70vnoy7swf
    access: proxy
    url: http://json-api:5000
    isDefault: true
    editable: true

  - name: Loki
    type: loki
    uid: loki-cloud-sec
    access: proxy
    url: http://loki:3100
    editable: true
    jsonData:
      maxLines: 1000
