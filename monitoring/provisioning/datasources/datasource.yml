apiVersion: 1

datasources:
  - name: Infinity
    type: yesoreyeram-infinity-datasource
    uid: infinity-uid
    access: proxy
    isDefault: true
    editable: false
    jsonData:
      allowedHosts:
        - http://json-api:5000

  - name: Loki
    type: loki
    uid: loki-cloud-sec
    url: http://loki:3100
    access: proxy
    editable: false
    jsonData:
      maxLines: 1000
